extends layout

block content 
    h1=articles.title
    h6 Written by #{articles.author}
    p= articles.body
    hr
    br
    a.btn.btn-default(href="/articles/edit/"+articles._id) Edit
    a.btn.btn-danger.delete-article(href='#',data-id=articles._id) Delete